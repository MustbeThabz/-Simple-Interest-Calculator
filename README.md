This project is a simple interest calculator implemented in JavaScript (Bash script in the original assignment, but we'll follow the task's required filename `simple-inventor.js`).

## Features
- Takes principal, rate of interest, and time period as input
- Computes simple interest
